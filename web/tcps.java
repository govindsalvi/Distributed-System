import java.net.*;import java.io.*;public class tcps {	public static void main(String[] args) throws Exception   {	String get1;	String get2;	String send;	ServerSocket ssocket=new ServerSocket(6789);	Socket connectionsocket=ssocket.accept();	BufferedReader	fromclient=new BufferedReader(new InputStreamReader(connectionsocket.getInputStream()));	DataOutputStream toclient=new DataOutputStream(connectionsocket.getOutputStream());	while(true)	{				        System.out.println("connection is ready");		get1=fromclient.readLine();		get2=fromclient.readLine();		int x=Integer.parseInt(get1);		int y=Integer.parseInt(get2);		x=x+y;                send=String.valueOf(x)+"\n";		//send=get.toUpperCase()+"\n";		toclient.writeBytes(send);	}	}}